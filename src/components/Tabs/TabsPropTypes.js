import React from 'react';
import PropTypes from 'prop-types';

const TabsPropTypes = ({ title, index }) => {
  return <div></div>;
};

TabsPropTypes.propTypes = {
  title: PropTypes.string,
  index: PropTypes.number,
};

export default TabsPropTypes;
